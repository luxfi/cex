import { container, title } from "../esx.js"

const appStyle = {
  stickyFooterRoot: {
    backgroundColor: "#e5e5e5",
    display: "flex",
    flexDirection: "column",
    minHeight: "100vh"
  },
  stickyFooter: {
    marginTop: "auto"
  }
}

export default appStyle
